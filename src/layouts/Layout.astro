---
export interface Props {
  title: string
}

const { title } = Astro.props as Props
---

<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/x-icon" href="/favicon.ico" />
    <meta name="generator" content={Astro.generator} />
    <meta name="theme-color" content="#171717" />
    <title>{title}</title>
    <link href="/code-theme.css" rel="stylesheet" />
  </head>
  <body>
    <slot />
    <style>
      @import "../styles/fonts";

      :root {
        --font-size-base: clamp(1rem, 0.34vw + 0.91rem, 1.19rem);
        --font-size-lg: clamp(1rem, 0.5vw + 1rem, 1.5rem);
        --font-size-xl: clamp(2rem, 2.38vw + 1.85rem, 3.75rem);
        --font-size-xxl: clamp(2.5rem, 3.5vw + 2rem, 8.5rem);

        --color-text: hsl(12, 5%, 4%);
      }

      html {
        font-family: "Inter", system-ui, sans-serif;
        font-size: var(--font-size-base);
        color: var(--color-text);
      }

      body {
        margin: 0;
      }

      :global(h1) {
        font-size: var(--font-size-xxl);
      }

      :global(h2) {
        font-size: var(--font-size-xl);
      }

      :global(h3) {
        font-size: var(--font-size-lg);
      }

      :global(code) {
        font-family: Menlo, Monaco, Lucida Console, Liberation Mono, DejaVu Sans Mono, Bitstream Vera Sans Mono,
          Courier New, monospace;
      }

      :global(.punctuation) {
        color: #52525b;
      }

      :global(.tagName) {
        color: #ec4899;
      }

      :global(.tagValue) {
        color: #f4f4f5;
      }

      :global(.attribute) {
        color: #d4d4d8;
      }

      :global(.attributeVal) {
        color: #8b5cf6;
      }
    </style>

    <!-- VISA Tracking Code for https://www.szegedia.com -->
    <script is:inline>
      ;(function (v, i, s, a, t) {
        v[t] =
          v[t] ||
          function () {
            ;(v[t].v = v[t].v || []).push(arguments)
          }
        if (!v._visaSettings) {
          v._visaSettings = {}
        }
        v._visaSettings[a] = { v: "1.0", s: a, a: "1", t: t }
        var b = i.getElementsByTagName("body")[0]
        var p = i.createElement("script")
        p.defer = 1
        p.async = 1
        p.src = s + "?s=" + a
        b.appendChild(p)
      })(window, document, "//app-worker.visitor-analytics.io/main.js", "f23f7bc9-285a-11ed-b589-901b0edac50a", "va")
    </script><!-- VISA Tracking Code for https://www.szegedia.com -->
  </body>
</html>
